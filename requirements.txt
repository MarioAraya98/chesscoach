# Dependencias del panel en Streamlit Community Cloud.
# La nube solo LEE data/games.db: el analisis con Stockfish corre en la PC.
chess>=1.11.1
httpx>=0.27
streamlit>=1.38
pandas>=2.2
altair>=5.4
pyyaml>=6.0

# El propio paquete: sin esto, `from chesscoach import ...` falla en la nube
# porque el codigo vive en src/ y no estaria en el sys.path.
.
